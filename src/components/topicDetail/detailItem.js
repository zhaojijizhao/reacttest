'use strict';

import React from 'react';
import ReactDom from 'react-dom';

var detailItem = React.createClass({
    render: function() {
        return (
            <div>
            </div>
        );
    }
});

export { detailItem } 
