'use strict';

import React from 'react';
import ReactDom from 'react-dom';

var newItem = React.createClass({
    render: function() {
        return (
            <div>
            </div>
        );
    }
});

export { newItem } 
