'use strict';

import React from 'react';
import ReactDom from 'react-dom';

var bottomBar = React.createClass({
    render: function() {
        return (
            <div>
            </div>
        );
    }
});

export { bottomBar } 
